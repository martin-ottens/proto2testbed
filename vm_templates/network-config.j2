network:
  version: 1
  config:
    - type: physical
      name: enp0s2
      subnets:
      - type: static
        address: {{ mgmt_address }}
        netmask: 255.255.255.0
        gateway: {{ mgmt_server }}
        dns_nameservers:
          - 8.8.8.8
          - 8.8.4.4
