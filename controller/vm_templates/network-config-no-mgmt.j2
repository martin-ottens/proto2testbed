network:
  version: 2
  ethernets:
  {% for interface in experiment_interfaces %}
    {{ interface.dev }}:
      match:
        macaddress: "{{ interface.mac }}"
      set-name: "{{ interface.dev }}"
  {% endfor %}
